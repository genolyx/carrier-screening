# Portal Configuration
PORTAL_URL=https://portal.genolyx.com
PORTAL_API_KEY=your_api_key_here
INSTITUTION_ID=genolyx

# Optional: Custom paths (if not using docker-compose volumes)
# PIPELINE_BASE_DIR=/home/ken/dark_gene_pipeline
