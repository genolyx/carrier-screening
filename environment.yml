name: base
channels:
  - conda-forge
  - bioconda
  - defaults
dependencies:
  - python=3.11
  - pip
  # Visualization tools (pre-installed to avoid runtime micromamba in PREPARE_VIZ_RESOURCES)
  - igv-reports
  - pysam
  - htslib
  - bwa
  - samtools
  - pip:
    - flask==3.0.0
    - requests==2.31.0
    - schedule==1.2.0
    - watchdog==3.0.0
    - gunicorn==21.2.0
